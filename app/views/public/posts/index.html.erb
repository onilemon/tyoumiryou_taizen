<h2>評価ランキング</h2>
<% @items.each.with_index(1) do |item, i| %>
  <%= image_tag item.image, size: "100x100" %>
  <%= i %>位
  <%= item.name %>
  評価<%= item.average %>
  <%= link_to "みんなの評価コメント", post_path(item.id) %><br>
<% end %>
